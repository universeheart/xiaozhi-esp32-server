# 开发记录

每个任务使用 `TASK-ID.md`，格式参照 [`../requirements/任务执行与验收模板.md`](../requirements/任务执行与验收模板.md)。记录只保存可复现摘要；可提交的脱敏测试摘要放入对应的 `../test-evidence/<TASK-ID>/`，并在任务记录中链接。

状态或代码基线变化时更新原任务文件，不另建含糊的“最终版”。真实 Secret、未经授权音频和完整敏感对话不得写入本目录。原始日志、音频、截图、APK、抓包和数据库备份不得提交到 Git。
