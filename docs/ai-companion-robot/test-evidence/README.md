# 测试证据目录

- 每个任务使用独立子目录 `test-evidence/<TASK-ID>/`。
- 文本结果应包含任务 ID、日期、目标 commit、环境版本、命令和退出码。
- 截图、音频、抓包和大日志只在确有验收价值时保存；文件名表达场景与序号。
- 任何凭据、Token、密码、完整个人对话、精确位置和未经授权音频必须先删除或脱敏。
- 本目录的 `.gitignore` 默认只允许提交 Markdown 摘要。大体积或原始产物不直接纳入源码版本库；任务记录保存哈希与受控位置。
